import 'package:flutter/material.dart';

class PharmacyList extends StatelessWidget {
  const PharmacyList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pharmacy list'),
    );
  }
}
