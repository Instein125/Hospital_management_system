import 'package:flutter/material.dart';

class PrescribeList extends StatelessWidget {
  const PrescribeList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('List'));
  }
}
