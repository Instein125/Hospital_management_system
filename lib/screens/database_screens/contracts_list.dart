import 'package:flutter/material.dart';

class ContractsList extends StatelessWidget {
  const ContractsList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("contract list"),
    );
  }
}
